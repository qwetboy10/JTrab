class List
{
    map(self, command)
    {
        var newList = []
        for(var i=0;i<self.length();i++)
        {
            newList.add(command(self.get(i)))
        }
        return newList
    }
}